apiVersion: batch/v1
kind: Job
metadata:
  name: kafka-producer1
spec:
  template:
    spec:
      containers:
      - name: kafka-producer1
        command:
          - sh
          - -c
          - "kafka-producer-perf-test --topic my-topic1 --num-records 25000000 --record-size 10 --throughput 10000000 --producer-props acks=1 buffer.memory=67108864 compression.type=lz4 batch.size=20000 linger.ms=100 retries=0 bootstrap.servers=my-cluster-kafka-bootstrap.myproject-dev:9092"