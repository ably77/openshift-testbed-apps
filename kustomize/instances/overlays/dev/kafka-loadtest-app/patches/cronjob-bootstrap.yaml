apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: cron-producer1
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: cron-producer1
            command:
              - sh
              - -c
              - "kafka-producer-perf-test --topic my-topic1 --num-records 2500000 --record-size 5 --throughput 10000000 --producer-props acks=1 buffer.memory=67108864 compression.type=lz4 batch.size=20000 linger.ms=100 retries=0 bootstrap.servers=my-cluster-kafka-bootstrap.myproject-dev:9092"
