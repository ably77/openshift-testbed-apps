apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# namespace to deploy all Resources to
namespace: codeready

bases:
- ../../../../../manifests/app/operators/codeready-workspaces-operator-unified/base

resources:
- codeready-namespace.yaml